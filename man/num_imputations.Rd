% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/num_imputations.R
\name{num_imputations}
\alias{num_imputations}
\title{Determine Number of Imputations Using Harrell's Rule}
\usage{
num_imputations(data)
}
\arguments{
\item{data}{A vector or data frame. Missing values (NA) are counted to
determine the fraction of missing data. For data frames, the fraction is
calculated column-wise and the maximum is used.}
}
\value{
An integer representing the recommended number of imputations. The
  minimum value is 5; for data with >5% missing, returns 100 times the
  fraction of missing values, rounded down. For data frames, uses the column
  with the highest missing fraction.
}
\description{
Calculates the recommended number of multiple imputations based on the
fraction of missing data. Implements Frank Harrell's guideline that the
number of imputations should be max(5, 100f) where f is the proportion of
missing values. For data frames, uses the maximum fraction across all columns.
}
\details{
This function implements Harrell's rule of thumb for multiple imputation:
use at least 5 imputations, or 100 times the fraction of missing observations,
whichever is larger. The rule is designed to ensure that key inferential
statistics are reproducible if the imputation analysis is repeated.

For vectors, the fraction f is calculated as the proportion of NA values.
For data frames, f is calculated for each column separately, and the maximum
fraction across all columns is used. This ensures sufficient imputations for
the variable with the most missing data.

For example:
- 3% missing → 5 imputations (minimum)
- 10% missing → 10 imputations
- 25% missing → 25 imputations

For minimal missing data (f < 0.03), simple imputation approaches may suffice.
}
\examples{
# Vector with 8\% missing data
x <- c(1, 2, NA, 4, 5, NA, 7, 8, 9, 10, 11, 12)
num_imputations(x)  # Returns 8

# Vector with only 2\% missing data (below minimum threshold)
y <- c(rep(1:10, 5), NA)
num_imputations(y)  # Returns 5 (minimum)

# Data frame with varying missing fractions by column
df <- data.frame(
  a = c(1:8, NA, NA),           # 20\% missing
  b = c(1:9, NA),                # 10\% missing
  c = 1:10                       # 0\% missing
)
num_imputations(df)  # Returns 20 (based on column 'a')

}
\references{
Harrell FE (2015). Regression Modeling Strategies, 2nd edition.
Springer-Verlag, New York. Chapter 3: Missing Data.
\url{https://hbiostat.org/rmsc/missing#summary-and-rough-guidelines}

White IR, Royston P, Wood AM (2011). Multiple imputation using chained
equations: Issues and guidance for practice. Statistics in Medicine,
30(4), 377-399.
}
